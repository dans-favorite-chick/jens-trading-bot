# Phoenix Strategy Oracle -- research Debrief (HALTED)

## Status
This run halted before producing a narrative.

## Regime
UNSTABLE -- analysis halted. Warning: Regime instability detected: latest-month sharpe-proxy z-score = +3.69 (threshold +/- 3.00). Baseline mean 0.035 over 6 months; latest 0.129 (2026-05). Analysis halted.

## Report Card
- 16 strategies analyzed
- 7 cleared all gates -> proposals
- 9 strategies failed gate psr_0_90
- 9 strategies failed gate dsr_0_95
- 9 strategies failed gate hlz_3_0
- 9 strategies failed gate min_trl_met
- 6 strategies failed gate wfa_pass
- 4 strategies failed gate bhy_0_05


---

## Red-Team Findings on the HALT verdict itself (this run)

Four red-team subagents reviewed this Oracle run.  Two had no blocking findings (R1 invocation auditor: PASS; R3 verifier coverage: as-designed — verifier didn't run because HALT was pre-LLM-loop; the post-divider addendum is structurally outside the verifier's input contract by design).

**R2 — regime_gate edge-case bug hunter: VERDICT = SUSPECT.**  R2's audit identified two CRITICAL and three HIGH-severity methodological concerns that could mean the HALT verdict is itself an artifact rather than a real regime-instability signal:

- **CRITICAL #1 — ingestion-lag artifact.** Between the 2026-06-01 Oracle run and this 2026-06-04 run, the May sharpe-proxy drifted from 0.1146 → 0.1294 — i.e., late-arriving May trades materially shifted a "historical" month's number.  If June trades are still landing or June is a partial month, the gate is comparing a partial-/lag-distorted "latest_month" against a baseline.
- **CRITICAL #2 — symmetric gate halts recovery months.** Research mode's loosened threshold (3.0 vs default 1.5) exists specifically so outlier-GOOD months don't block deep dives; +3.69 sits only 0.69σ above that, well within the noise floor R2 identified.
- **HIGH #1 — baseline contamination.** The 6-month baseline excludes only the latest month, so if MQ regime gradually improved over Q1–Q2 2026, the baseline mixes pre- and post-shift months — sample std deflates, z inflates.
- **HIGH #2 — gate is portfolio-aggregate.** A single big-PF strategy (e.g., `a_asian_continuation` PF=3.42 over 1,878 trades in the same facts panel) can dominate the all-strategy sharpe-proxy and mask per-strategy regime shifts (false PASS direction); the same dominance amplifies a single hot strategy month into a portfolio-wide HALT (false HALT direction).
- **MEDIUM — "sharpe-proxy" lacks trade-count normalization.** Avg/std of per-trade PnL with no √N adjustment compares 80-trade and 800-trade months at face value.

R2's diagnostic recommendation, verbatim:

> Regenerate the gate excluding any month with `< (median trade_count * 0.5)` trades AND require `baseline_n >= 6`. If z still > 3.0 after that, the regime shift is real; if it collapses below 2.0, the HALT was a Finding-1+3 artifact and parameter-change work can proceed.

**Operationally:** the spec says "score CRITICAL findings as HALT, HIGH+ logged."  Two CRITICAL findings on the gate itself mean **the operator should NOT treat tonight's `halted_regime_unstable` verdict as load-bearing without first running R2's diagnostic re-run** (filtered-month baseline + minimum-n requirement).  The PHANTOM-NT8 freeze remains the dominant blocker either way — neither this HALT nor a hypothetical PASS would license a parameter flip while the replay-loop bug is live — but the operator's interpretation of "regime is unstable, wait for it to stabilize" may be wrong: the regime may already be fine and the gate may be triggering on its own measurement artifacts.

R2's full audit is in subagent transcript `a87113094ef8daf47` (red-team session).  R3 verifier coverage audit in `a49dcc7dd84bf2769`.  R1 invocation audit (PASS) in `a5a0ea60b75b7f49d`.  R4 determinism live smoke is the remaining open thread; if R4 finds non-determinism that further compounds R2's concern.

### Recommended follow-up (this sprint only files findings; does NOT fix gates)

1. New finding: `FINDING-2026-06-04-ORACLE-REGIME-GATE-METHODOLOGY` — references R2's two CRITICAL items.  Status OPEN.  Owner = next Oracle-touching sprint.
2. R2's diagnostic re-run could be a 1-hour follow-up if operator wants to know whether tonight's HALT was real.  Below the bar for blocking PHANTOM-NT8 (which is the dominant production-safety blocker).
---

## Sprint Addendum (not Oracle-generated — appended post-run by re-run sprint)

**Source:** 2026-06-04 Winning Conditions ORACLE RE-RUN sprint. The Oracle-generated section above is the canonical, verifier-eligible output. This addendum is operator context that does not flow through the verifier.

### PHANTOM-NT8 blocker (active replay loop, parallel sprint)

The parallel **PHANTOM-NT8 sprint** identified an active OIF replay loop on prod_bot Sim101: stale RECONCILED-tagged OIFs from a 19h27m NT8 outage (06-03 22:20:53 → 06-04 17:47:51 CDT) are firing as live SHORT positions on the next NT8 reconnect, bypassing every sizing / interlock control. The operator manually flattened 3 clusters tonight (5:50, 5:54, 6:01 PM CDT — including a 7-contract cluster that was profitable by direction luck, not sizing intent).

**Implication for this Oracle run's downstream use:**

- Any parameter envelope this Oracle would have produced (if Phase 2 had not HALTed) would have been **provisional until PHANTOM-NT8 ships**.
- A parameter change deployed before the replay-loop fix would be masked by stale-OIF noise — the change couldn't be cleanly attributed to its intent.
- Operator: **do NOT advance any `pending_changes.json` proposal through the Phase 4E approval flow until PHANTOM-NT8 is RESOLVED**.

### Why the regime_gate HALT is the operationally correct verdict

- Latest month (2026-05) sharpe-proxy: 0.129
- 6-month baseline mean: 0.035
- Z-score: +3.69 (vs research-mode threshold ±3.00; default 1.5 in weekly/daily)
- Direction: **outlier-GOOD**, not bad. Recent performance is unusually strong vs trailing baseline.

Parameter tuning on a window that includes a +3.69σ outlier month is the classic overfitting trap. The deterministic warehouse facts panel shows both `bias_momentum` (n=28,507, PF=1.28, all gates pass) and `opening_session` (n=3,703, PF=1.44, all gates pass) are 5-year robust strategies — they don't need rescue. Tuning their parameters to "lock in" a 1-month outlier would degrade their longer-horizon performance.

### Cross-reference to ad-hoc sprint artifacts

The earlier ad-hoc winning-conditions sprint (commit `9e72ae5`) produced markdown reports at `out/winning_conditions_*_2026-06-04.md` plus charts at `out/charts/winning_conditions/*.png`. Those artifacts have been moved to `out/winning_conditions_ad_hoc_2026-06-04/` to preserve the audit trail while making clear they are SUPERSEDED by this canonical Oracle output. The ad-hoc sprint's conclusions converge with Oracle's: it independently flagged that the post-Apr-18 tightening was working (counterfactual at relaxed params lost money), which is consistent with Oracle's regime-stability concern that today is not the moment to re-tune.

### What the operator should do next

1. **Ship the PHANTOM-NT8 fix first** (out of scope for this sprint; tracked at `FINDING-2026-06-04-PHANTOM-NT8`).
2. **Re-run Oracle research mode** after ~30 more days of trading. The rolling baseline will absorb the 2026-05 outlier and the z-score should normalize. If it doesn't normalize, the regime has genuinely shifted and the conversation is about which strategies are still appropriate for the new regime — not parameter tuning.
3. **Do NOT advance any `pending_changes.json` entry through Phase 4E approval flow** while either (a) PHANTOM-NT8 is open or (b) regime_gate is HALTING. The FREEZE_ACTIVE flag at `config/strategies.py:52` is the existing interlock that enforces this; do not flip it.

### Look-ahead invariance note

This sprint shipped **zero production code or config changes**. The Oracle's `pending_changes.json` is unchanged from its prior state (Oracle did not stage any new proposals because the LLM loop did not run past the regime HALT). The only file additions are: this debrief addendum, the new facts.json + (empty) audit.jsonl from Oracle, and the moved ad-hoc artifacts.

### R4 determinism live smoke — VERDICT: DETERMINISTIC (semantically)

Subagent transcript `a4d1602d330950577`. Second invocation of `run("research")` at 22:25 (4 min after the first 22:21 run). Comparison:

- **HALT verdict identical** — both runs: `halted_regime_unstable`.
- **`regime` panel identical to the last digit** — `z_score=3.6900902788364744`, `stable=false`, `latest_sharpe_proxy=0.12943916933053984`, byte-identical warning string.
- **`audit.jsonl` byte-identical** (both empty, 0 bytes — expected per HALT-before-LLM-loop).
- **`debrief.md` Oracle-generated section** identical after `.strip()` (1-byte trailing-whitespace difference: 625 vs 626 bytes).
- **`facts.json` hash differs by 12 bytes.** Deep diff (16 strategies, ~600 KB JSON) isolated the sole difference to 10 `profit_factor: NaN` fields across `es_nq_confluence`, `a_asian_continuation`, `compression_breakout_micro`, `vwap_band_reversion`, `raschke_baseline` splits. Python's `NaN != NaN` invariant; normalizing NaN to a sentinel proves the two dicts equal.

**Verdict surface is stable.** R4 does NOT compound R2's CRITICAL concerns — Oracle's gate produces the SAME measurement deterministically, even if R2 argues that measurement itself may be an artifact of partial-month ingestion lag and baseline contamination.

**Small housekeeping fix recommended** (filed as `FINDING-2026-06-04-ORACLE-JSON-NONDETERMINISM` in the tracker): switch `_write_facts()` to `json.dumps(facts, indent=2, sort_keys=True, allow_nan=False, default=str)` and replace NaN with `null` at serialization time so two same-result runs produce byte-identical `facts.json`. Not a CRITICAL bug; cosmetic stability.
